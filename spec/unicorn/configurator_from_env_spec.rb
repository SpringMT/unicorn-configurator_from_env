require 'spec_helper'

describe Unicorn::ConfiguratorFromEnv do
  it 'has a version number' do
    expect(Unicorn::ConfiguratorFromEnv::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
